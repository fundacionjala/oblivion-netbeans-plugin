public with sharing class KeywordOnLoops {
    public void loops() {
        for(integer without =0 ; without < 10; without++) {
            offset = without;
        }
        integer offset = 10;
        while(offset>0) {
            offset--;
        }
    }
}
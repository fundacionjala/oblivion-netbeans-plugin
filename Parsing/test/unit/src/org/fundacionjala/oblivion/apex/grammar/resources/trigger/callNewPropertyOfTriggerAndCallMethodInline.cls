public class MyClass {
    public void myMethod() {
	Integer maxSize = Trigger.new.size();
	Integer minSize = System.Trigger.new.size();
	Integer currentSize = !Trigger.isInsert ? Trigger.old.size() : Trigger.new.size();
    }
}

